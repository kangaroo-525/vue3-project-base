import type { Router } from 'vue-router'

export function createPageLoadingGuard(router: Router) {
  router.beforeEach(() => {
    // todo
  })

  router.afterEach(() => {
    // todo
  })

  router.onError(() => {
    // todo
  })
}
